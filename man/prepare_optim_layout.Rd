% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_optim_layout.R
\name{prepare_optim_layout}
\alias{prepare_optim_layout}
\title{Prepare optimization layout for a model}
\usage{
prepare_optim_layout(model)
}
\arguments{
\item{model}{A \code{time_series_model} or \code{sum_model}.}
}
\value{
A list with:
\itemize{
\item \code{kind}: \code{"single"} or \code{"sum"} depending on the model class.
\item \code{theta0}: named numeric vector of initial parameters in real space.
\item \code{pnames}: (single model) parameter names for the block.
\item \code{layout}: (sum model) list of per-component layouts, each containing:
\code{i} (component index), \code{idx} (indices into \code{theta0}), and \code{pnames}
(parameter names for that component).
}
}
\description{
Build a bookkeeping structure that maps a flat optimization vector
(\code{theta}, in real/unconstrained space) to per-component parameter blocks.
This is used throughout the package to:
\itemize{
\item Define a consistent parameter order for optimization.
\item Provide an initial parameter vector (\code{theta0}) in real space.
\item Record which subset of \code{theta} belongs to each component model.
}
}
\details{
The function handles two cases:
\itemize{
\item \code{time_series_model}: returns a single block of parameters.
\item \code{sum_model}: concatenates all component blocks and records their indices.
}
}
\keyword{internal}
