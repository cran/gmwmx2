% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{generate}
\alias{generate}
\title{Generate a time series from a \code{time_series_model} or \code{sum_model} object}
\usage{
generate(object, n, seed = NULL, ...)
}
\arguments{
\item{object}{A \code{time_series_model} or \code{sum_model}.}

\item{n}{Length of series to generate.}

\item{seed}{Optional integer seed for reproducibility.}

\item{...}{Passed to method.}
}
\value{
A \code{generated_time_series} (single model) or
\code{generated_composite_model_time_series} (sum model).
}
\description{
Generate a time series from a \code{time_series_model} or \code{sum_model} object
}
\examples{
# Single model
m1 <- ar1(phi = 0.8, sigma2 = 1)
y1 <- generate(m1, n = 200, seed = 123)
plot(y1)

# Composite model
m2 <- wn(sigma2 = 1) + pl(kappa = 0.3, sigma2 = 2)
y2 <- generate(m2, n = 200, seed = 123)
plot(y2)
}
